[pytest]
minversion = 7.0
addopts = -v --tb=short --strict-markers --disable-warnings --html=tests/output/report.html --self-contained-html
testpaths = tests
timeout = 600
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests