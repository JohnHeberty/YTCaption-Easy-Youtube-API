[pytest]
testpaths = .
asyncio_mode = auto
markers =
    e2e: end-to-end API validation tests
    health: health endpoint tests
    crud: CRUD endpoint tests
    admin: admin endpoint tests
    auth: authentication tests
