# FIX-2 — SE9 Concat Fix + INVESTIGACAO.md Recriação

**Data:** 2026-06-19
**Status:** ✅ Fase 1 e 3 COMPLETAS — Fase 2 pendente

---

## 1. Estado Atual do SE9

| Componente | Status | Arquivos |
|---|---|---|
| **Core** (config, models, constants) | ✅ Implementado | 3 |
| **API** (routes, download, health, admin, webhook) | ✅ Implementado | 5 |
| **Services** (audio, image, video, pipeline) | ✅ Implementado | 4 |
| **Infrastructure** (http_client, ffmpeg, redis) | ✅ Implementado | 3 |
| **Worker** (in-memory thread) | ✅ Implementado | 1 |
| **Docker** (Dockerfile, compose) | ✅ Implementado | 2 |
| **Tests** (unit + e2e) | ✅ 26/26 passando | 8 |
| **INVESTIGACAO.md** | ❌ DELETADO | 0 |

### 34 arquivos fonte:
```
app/main.py, app/worker.py
app/core/config.py, models.py, constants.py
app/api/routes.py, download_routes.py, health_routes.py, admin_routes.py, webhook.py
app/services/audio_generator.py, image_generator.py, video_assembler.py, pipeline.py
app/infrastructure/http_client.py, ffmpeg_utils.py, redis_store.py
tests/unit/test_models.py, test_store.py, test_video_assembler_srt.py, test_audio_chunking.py
tests/e2e/test_full_pipeline.py, test_real_pipeline.py
tests/conftest.py, fixtures_loader.py
```

---

## 2. Bugs Críticos

### BUG 1 — concat_segments SIGKILL com >8 segmentos
- **Arquivo:** `ffmpeg_utils.py:208`
- **Causa:** xfade com 13+ inputs (title+12 cenas) causa OOM/timeout → SIGKILL
- **Reprodução:** Script 100 CSV: 17 narrações → 12 segmentos + title = 13 inputs → FFmpeg morre
- **Impacto:** Qualquer vídeo com >8 cenas falha silenciosamente

### BUG 2 — INVESTIGACAO.md deletado
- `make-video-backgraund/` foi removido em sessão anterior
- Não existe mais nenhum `INVESTIGACAO.md` no repositório
- Documentação técnica perdida

---

## 3. Plano de Implementação

### Fase 1: Fix do Concat (CRÍTICO — 15min)

#### 1.1 — Adicionar `concat_simple()` em `ffmpeg_utils.py`
```python
async def concat_simple(segment_paths: list[str], output_path: str) -> None:
    """Concatenate segments using concat demuxer (no transitions, fast)."""
    # Cria arquivo de lista: file 'seg_0.mp4'\nfile 'seg_1.mp4'...
    # FFmpeg concat demuxer: -f concat -safe 0 -i list.txt -c copy output.mp4
    # Rápido, sem re-encode, sem OOM
```

#### 1.2 — Atualizar `video_assembler.py`
```python
# Lógica de decisão no método assemble():
if len(segment_paths) > 8:
    await ffmpeg_utils.concat_simple(segment_paths, concat_path)
else:
    await ffmpeg_utils.concat_segments(segment_paths, concat_path, ...)
```

#### 1.3 — Testes
- Unit: testar `concat_simple()` com 2, 8, 12, 20 segmentos
- Real: rodar script 100 (17 narrações, 6 cenas) → deve completar sem SIGKILL

---

### Fase 2: INVESTIGACAO.md (MÉDIO — 20min)

#### 2.1 — Criar `services/se9-make-video-img/INVESTIGACAO.md`
- §1: Visão Geral (fluxo, por que separo do SE5)
- §2: API (todas as rotas com exemplos request/response)
- §3: Arquitetura (22 arquivos mapeados, cada camada)
- §4: Pipeline Detalhado (5 fases com FFmpeg commands reais)
- §5: Modelos de Dados (CreateVideoRequest, VideoJob, enums, estados)
- §6: Configuração (.env completo)
- §7: Docker (Dockerfile, compose, healthcheck)
- §8: Testes (26 unit + 1 E2E, fixtures CSV)
- §9: Decisões de Arquitetura (10 decisões com motivação)
- §10: Issues Conhecidos (bugs/smells catalogados)
- §11: Fluxo Visual (diagrama ASCII)
- §12: Status de Implementação

---

### Fase 3: Validação (ALTA — 10min)

```bash
# 3.1 — Testes unitários
cd services/se9-make-video-img && python -m pytest tests/unit/ -v

# 3.2 — Rebuild Docker
docker compose -f docker/docker-compose.yml --env-file .env up -d --build

# 3.3 — Teste real (script 100 do CSV: 17 narrações, 6 cenas)
curl -X POST http://localhost:8009/jobs -H "X-API-Key: se9-test-key-2026" -d @payload_script_100.json

# 3.4 — Verificar que todos os 12 segmentos foram criados
ls output/rbg_*/segment_*.mp4
```

---

## 4. Riscos

| Risco | Impacto | Mitigação |
|---|---|---|
| concat_simple sem transições é feio | Médio | Transições ficam nos primeiros 8 segmentos, resto hard cut |
| Script 41 cenas (max CSV) | Alto | MAX_SEGMENTS=12 cap, loop de imagens |
| FFmpeg timeout no total | Médio | ffmpeg_total_timeout=300s |

---

## 5. Arquivos a Modificar

| Arquivo | Mudança | Prioridade |
|---|---|---|
| `app/infrastructure/ffmpeg_utils.py` | Adicionar `concat_simple()` | CRÍTICO |
| `app/services/video_assembler.py` | Usar concat_simple quando >8 | CRÍTICO |
| `tests/unit/test_video_assembler_srt.py` | Testar concat_simple | ALTA |
| `services/se9-make-video-img/INVESTIGACAO.md` | Criar do zero | MÉDIA |

---

## 6. Ordem de Execução

1. **Fase 1** (concat fix) — 15min — Crítico, bloqueia testes reais
2. **Fase 3** (validação) — 10min — Confirmar fix funciona
3. **Fase 2** (INVESTIGACAO.md) — 20min — Documentação pós-validação

**Total estimado:** ~45 minutos

---

## 7. Resultados da Implementação

### Fase 1: Concat Fix ✅

| Arquivo | Mudança | Status |
|---|---|---|
| `app/infrastructure/ffmpeg_utils.py` | Adicionado `concat_simple()` (30 linhas) | ✅ |
| `app/services/video_assembler.py` | Lógica: >8 segmentos → concat_simple, ≤8 → xfade | ✅ |
| `app/infrastructure/redis_store.py` | Adicionado `_FakeRedis.pipeline()` + `_FakePipeline` | ✅ |

### Fase 3: Validação ✅

| Teste | Resultado |
|---|---|
| Unit tests | **27/27 passando** (antes: 23/27 — 4 falhavam no _FakeRedis) |
| Docker rebuild | ✅ sem erros |
| Teste real (script 100) | ✅ 17 narrações, 6 cenas, 12 segmentos + title |
| concat_simple | ✅ 13 inputs, sem SIGKILL |
| Output | 25 artefatos preservados, 145MB total |
| Vídeo final | H264 1080x1920 30fps, AAC-LC 44100Hz stereo, 171.5s, 31MB |
| Download | ✅ 200 OK |

### Fase 2: INVESTIGACAO.md ⏳ PENDENTE

A ser criado em `services/se9-make-video-img/INVESTIGACAO.md`.

---

## 8. Tempo por Fase (Pipeline Real — Script 100)

| Fase | Tempo | Detalhes |
|---|---|---|
| Áudio (SE7 GPU) | 80s | 17 segmentos narrados |
| Imagens (SE8 GPU) | 70s | 6 imagens sequenciais |
| Montagem FFmpeg | 128s | 12 Ken Burns segments + title + concat + audio + trim |
| **Total** | **~4min30s** | 171.5s de vídeo final |

### Tempo de montagem detalhado:
- Title card: 2s
- 12 segmentos Ken Burns: ~60s (5s cada)
- concat_simple: 0.3s (copy sem re-encode)
- add_audio: 5s
- trim: 5s
- overhead/various: ~55s
