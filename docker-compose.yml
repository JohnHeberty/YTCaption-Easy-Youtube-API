services:
  # Audio Normalization Service
  se3-audio-normalization:
    build: ./services/se3-audio-normalization
    container_name: ytcaption-se3-audio-normalization
    ports:
      - "8003:8003"
    volumes:
      - ./services/se3-audio-normalization/data/uploads:/app/data/uploads
      - ./services/se3-audio-normalization/data/processed:/app/data/processed
      - ./services/se3-audio-normalization/data/temp:/app/data/temp
      - ./services/se3-audio-normalization/data/logs:/app/data/logs
    environment:
      - PYTHONPATH=/app
      - PORT=8003
      - REDIS_URL=redis://192.168.1.110:6379/0
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - UPLOAD_DIR=/app/data/uploads
      - OUTPUT_DIR=/app/data/processed
      - TEMP_DIR=/app/data/temp
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  se3-audio-normalization-celery:
    build: ./services/se3-audio-normalization
    container_name: ytcaption-se3-audio-normalization-celery
    command: python -m celery -A app.infrastructure.celery_config worker --loglevel=info --concurrency=1 --pool=solo --queues=audio_normalization_queue
    volumes:
      - ./services/se3-audio-normalization/app:/app/app
      - ./services/se3-audio-normalization/data/uploads:/app/data/uploads
      - ./services/se3-audio-normalization/data/processed:/app/data/processed
      - ./services/se3-audio-normalization/data/temp:/app/data/temp
      - ./services/se3-audio-normalization/data/logs:/app/data/logs
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://192.168.1.110:6379/0
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - UPLOAD_DIR=/app/data/uploads
      - OUTPUT_DIR=/app/data/processed
      - TEMP_DIR=/app/data/temp
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    depends_on:
      se3-audio-normalization:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -m celery -A app.infrastructure.celery_config inspect ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  # Audio Transcriber Service
  se4-audio-transcriber:
    build: ./services/se4-audio-transcriber
    container_name: ytcaption-se4-audio-transcriber
    ports:
      - "8004:8004"
    volumes:
      - ./services/se4-audio-transcriber/data/uploads:/app/data/uploads
      - ./services/se4-audio-transcriber/data/transcriptions:/app/data/transcriptions
      - ./services/se4-audio-transcriber/data/models:/app/data/models
      - ./services/se4-audio-transcriber/data/temp:/app/data/temp
      - ./services/se4-audio-transcriber/data/logs:/app/data/logs
    devices:
      - /dev/nvidia0:/dev/nvidia0
      - /dev/nvidiactl:/dev/nvidiactl
      - /dev/nvidia-uvm:/dev/nvidia-uvm
      - /dev/nvidia-uvm-tools:/dev/nvidia-uvm-tools
    env_file:
      - ./services/se4-audio-transcriber/.env
    environment:
      - TZ=America/Sao_Paulo
      - PYTHONPATH=/app
      - NVIDIA_VISIBLE_DEVICES=
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  se4-audio-transcriber-celery:
    build: ./services/se4-audio-transcriber
    container_name: ytcaption-se4-audio-transcriber-celery
    command: python -m celery -A app.celery_config worker --loglevel=info --concurrency=1 --pool=solo --queues=audio_transcriber_queue
    volumes:
      - ./services/se4-audio-transcriber/app:/app/app
      - ./services/se4-audio-transcriber/data/uploads:/app/data/uploads
      - ./services/se4-audio-transcriber/data/transcriptions:/app/data/transcriptions
      - ./services/se4-audio-transcriber/data/models:/app/data/models
      - ./services/se4-audio-transcriber/data/temp:/app/data/temp
      - ./services/se4-audio-transcriber/data/logs:/app/data/logs
    # GPU support requer nvidia-container-toolkit no host Proxmox
    # devices:
    #   - /dev/nvidia0:/dev/nvidia0
    #   - /dev/nvidiactl:/dev/nvidiactl
    #   - /dev/nvidia-uvm:/dev/nvidia-uvm
    env_file:
      - ./services/se4-audio-transcriber/.env
    environment:
      - TZ=America/Sao_Paulo
      - PYTHONPATH=/app
      - C_FORCE_ROOT=true
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    depends_on:
      se4-audio-transcriber:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -m celery -A app.celery_config inspect ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  # Video Downloader Service
  se2-video-downloader:
    build: ./services/se2-video-downloader
    container_name: ytcaption-se2-video-downloader
    ports:
      - "8002:8002"
    volumes:
      - ./services/se2-video-downloader/data/cache:/app/data/cache
      - ./services/se2-video-downloader/data/logs:/app/data/logs
      - ./services/se2-video-downloader/user-agents.txt:/app/user-agents.txt
      - ./services/se2-video-downloader/user-agents-clean.txt:/app/user-agents-clean.txt
    environment:
      - PYTHONPATH=/app
      - PORT=8002
      - REDIS_URL=redis://192.168.1.110:6379/0
      - UA_QUARANTINE_HOURS=48
      - UA_MAX_ERRORS=3
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - CACHE_DIR=/app/data/cache
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  se2-video-downloader-celery:
    build: ./services/se2-video-downloader
    container_name: ytcaption-se2-video-downloader-celery
    command: python -m celery -A app.infrastructure.celery_config worker --loglevel=info --concurrency=1 --pool=solo --queues=video_downloader_queue
    volumes:
      - ./services/se2-video-downloader/app:/app/app
      - ./services/se2-video-downloader/data/cache:/app/data/cache
      - ./services/se2-video-downloader/data/logs:/app/data/logs
      - ./services/se2-video-downloader/user-agents.txt:/app/user-agents.txt
      - ./services/se2-video-downloader/user-agents-clean.txt:/app/user-agents-clean.txt
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://192.168.1.110:6379/0
      - UA_QUARANTINE_HOURS=48
      - UA_MAX_ERRORS=3
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - CACHE_DIR=/app/data/cache
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    depends_on:
      se2-video-downloader:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -m celery -A app.infrastructure.celery_config inspect ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

networks:
  ytcaption-network:
    driver: bridge
    name: ytcaption-network
