services:
  # Audio Normalization Service
  audio-normalization:
    build: ./services/audio-normalization
    container_name: ytcaption-audio-normalization
    ports:
      - "8001:8001"
    volumes:
      - ./services/audio-normalization/uploads:/app/uploads
      - ./services/audio-normalization/processed:/app/processed
      - ./services/audio-normalization/temp:/app/temp
      - ./services/audio-normalization/logs:/app/logs
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://192.168.1.110:6379/0
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - UPLOAD_DIR=/app/uploads
      - OUTPUT_DIR=/app/processed
      - TEMP_DIR=/app/temp
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  audio-normalization-celery:
    build: ./services/audio-normalization
    container_name: ytcaption-audio-normalization-celery
    command: python -m celery -A app.celery_config worker --loglevel=info --concurrency=1 --pool=solo --queues=audio_normalization_queue
    volumes:
      - ./services/audio-normalization/uploads:/app/uploads
      - ./services/audio-normalization/processed:/app/processed
      - ./services/audio-normalization/temp:/app/temp
      - ./services/audio-normalization/logs:/app/logs
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://192.168.1.110:6379/0
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - UPLOAD_DIR=/app/uploads
      - OUTPUT_DIR=/app/processed
      - TEMP_DIR=/app/temp
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    depends_on:
      audio-normalization:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -m celery -A app.celery_config inspect ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  # Audio Transcriber Service
  audio-transcriber:
    build: ./services/audio-transcriber
    container_name: ytcaption-audio-transcriber
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - ./services/audio-transcriber/uploads:/app/uploads
      - ./services/audio-transcriber/transcriptions:/app/transcriptions
      - ./services/audio-transcriber/models:/app/models
      - ./services/audio-transcriber/temp:/app/temp
      - ./services/audio-transcriber/logs:/app/logs
    devices:
      - /dev/nvidia0:/dev/nvidia0
      - /dev/nvidiactl:/dev/nvidiactl
      - /dev/nvidia-uvm:/dev/nvidia-uvm
      - /dev/nvidia-uvm-tools:/dev/nvidia-uvm-tools
    env_file:
      - ./services/audio-transcriber/.env
    environment:
      - TZ=America/Sao_Paulo
      - PYTHONPATH=/app
      - NVIDIA_VISIBLE_DEVICES=
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT}/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  audio-transcriber-celery:
    build: ./services/audio-transcriber
    container_name: ytcaption-audio-transcriber-celery
    command: python -m celery -A app.celery_config worker --loglevel=info --concurrency=1 --pool=solo --queues=audio_transcriber_queue
    volumes:
      - ./services/audio-transcriber/uploads:/app/uploads
      - ./services/audio-transcriber/transcriptions:/app/transcriptions
      - ./services/audio-transcriber/models:/app/models
      - ./services/audio-transcriber/temp:/app/temp
      - ./services/audio-transcriber/logs:/app/logs
    # GPU support requer nvidia-container-toolkit no host Proxmox
    # devices:
    #   - /dev/nvidia0:/dev/nvidia0
    #   - /dev/nvidiactl:/dev/nvidiactl
    #   - /dev/nvidia-uvm:/dev/nvidia-uvm
    env_file:
      - ./services/audio-transcriber/.env
    environment:
      - TZ=America/Sao_Paulo
      - PYTHONPATH=/app
      - C_FORCE_ROOT=true
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    depends_on:
      audio-transcriber:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -m celery -A app.celery_config inspect ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  # Video Downloader Service
  video-downloader:
    build: ./services/video-downloader
    container_name: ytcaption-video-downloader
    ports:
      - "8000:8001"
    volumes:
      - ./services/video-downloader/cache:/app/cache
      - ./services/video-downloader/logs:/app/logs
      - ./services/video-downloader/user-agents.txt:/app/user-agents.txt
      - ./services/video-downloader/user-agents-clean.txt:/app/user-agents-clean.txt
      - ./services/video-downloader/user-agents-original.txt:/app/user-agents-original.txt
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://192.168.1.110:6379/0
      - UA_QUARANTINE_HOURS=48
      - UA_MAX_ERRORS=3
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - CACHE_DIR=/app/cache
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

  video-downloader-celery:
    build: ./services/video-downloader
    container_name: ytcaption-video-downloader-celery
    command: python -m celery -A app.celery_config worker --loglevel=info --concurrency=1 --pool=solo --queues=video_downloader_queue
    volumes:
      - ./services/video-downloader/cache:/app/cache
      - ./services/video-downloader/logs:/app/logs
      - ./services/video-downloader/user-agents.txt:/app/user-agents.txt
      - ./services/video-downloader/user-agents-clean.txt:/app/user-agents-clean.txt
      - ./services/video-downloader/user-agents-original.txt:/app/user-agents-original.txt
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://192.168.1.110:6379/0
      - UA_QUARANTINE_HOURS=48
      - UA_MAX_ERRORS=3
      - CACHE_TTL_HOURS=24
      - CLEANUP_INTERVAL_MINUTES=30
      - LOG_LEVEL=INFO
      - CACHE_DIR=/app/cache
    extra_hosts:
      - "redis-server:192.168.1.110"
    restart: unless-stopped
    depends_on:
      video-downloader:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -m celery -A app.celery_config inspect ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - ytcaption-network

networks:
  ytcaption-network:
    external: true
    name: ytcaption-network