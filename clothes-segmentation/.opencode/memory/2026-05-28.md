
## 11:08 - Phase 2: External repos moved (completed)

## 11:15 - User decision: removed data/ and imgs/ — API will receive images via HTTP, no need for local test image storage
## 20:57 - Makefile.ps1 encoding fix

Rewrote with function-based dispatch. Must invoke via: powershell -ExecutionPolicy Bypass -File Makefile.ps1 -Target <command>. Tested: checkpoints, deps-check, help, clean — all OK.
